<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>{{ meta.title }}</title>
        <link rel="stylesheet" href="{{ '/assets/styles/bootstrap.min.css' | relative }}" />
        <link rel="stylesheet" href="{{ 'src/assets/styles/app.scss' | hash | relative }}" />
    </head>
    <body>
        {{ content | safe }}
        <script src="{{ 'src/assets/js/app.js' | hash | relative }}"></script>
    </body>
</html>
